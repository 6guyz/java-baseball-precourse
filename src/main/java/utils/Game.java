package utils;

import java.util.Scanner;

public class Game {
    final Scanner scanner = new Scanner(System.in);

    private int targetNumber;

    // game instance 생성 시
    // targetNumber 생성
    public Game() {
        targetNumber = (int)(Math.random() * 889) + 111;
    }

    public void start() {
        // 사용자 입력

        // userNumber validation check

        // targetNumber userNumber 값 비교

        // 비교 결과 출력

        // 종료 여부 선택


    }
}
